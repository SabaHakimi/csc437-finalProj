/** Client-side routes that should serve index.html (SPA fallback). Keep in sync with frontend App.jsx routes. */
export const VALID_ROUTES = {
    HOME: "/",
    LOGIN: "/login",
    RECIPES: "/recipes",
    RECIPES_ADD: "/recipes/add",
    ADD: "/add",
};
